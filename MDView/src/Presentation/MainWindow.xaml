<!-- MDView/src/Presentation/MainWindow.xaml -->
<Window
    x:Class="MDView.Presentation.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wv2="using:Microsoft.Web.WebView2.Core"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border x:Name="AppTitleBar"
                Grid.Row="0"
                Height="48"
                Padding="16,0"
                Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Logo + Title -->
                <StackPanel Grid.Column="0"
                            Orientation="Horizontal"
                            Spacing="10"
                            VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE8A5;"
                              FontSize="18"
                              Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    <TextBlock x:Name="TitleText"
                               Text="MDView"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               FontWeight="SemiBold"
                               FontSize="13"
                               VerticalAlignment="Center" />
                </StackPanel>

                <!-- File Path Breadcrumb -->
                <TextBlock x:Name="FilePathText"
                           Grid.Column="1"
                           Text=""
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           TextTrimming="CharacterEllipsis" />

                <!-- Toolbar Buttons -->
                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            Spacing="4"
                            VerticalAlignment="Center">

                    <Button x:Name="OpenFileButton"
                            Click="OnOpenFileClick"
                            Style="{StaticResource DefaultButtonStyle}"
                            Padding="10,6"
                            ToolTipService.ToolTip="Open Markdown file (Ctrl+O)">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon Glyph="&#xE8E5;" FontSize="14" />
                            <TextBlock Text="Open" FontSize="13" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="ReloadButton"
                            Click="OnReloadClick"
                            Style="{StaticResource DefaultButtonStyle}"
                            Padding="8,6"
                            IsEnabled="False"
                            ToolTipService.ToolTip="Reload (F5)">
                        <FontIcon Glyph="&#xE72C;" FontSize="14" />
                    </Button>

                    <ToggleButton x:Name="ThemeToggleButton"
                                  Click="OnThemeToggleClick"
                                  Padding="8,6"
                                  ToolTipService.ToolTip="Toggle Light / Dark theme">
                        <FontIcon x:Name="ThemeIcon" Glyph="&#xE793;" FontSize="14" />
                    </ToggleButton>

                </StackPanel>
            </Grid>
        </Border>

        <!-- WebView2 Renderer -->
        <Grid Grid.Row="1">
            <!-- Drop Target Overlay (shown when no file loaded) -->
            <Border x:Name="DropOverlay"
                    AllowDrop="True"
                    DragOver="OnDragOver"
                    Drop="OnDrop"
                    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Spacing="16">
                    <FontIcon Glyph="&#xE8A5;"
                              FontSize="64"
                              Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                    <TextBlock Text="Drop a Markdown file here"
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="or click Open to browse"
                               Style="{StaticResource BodyTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                               HorizontalAlignment="Center" />
                    <Button Content="Open File"
                            Click="OnOpenFileClick"
                            Style="{StaticResource AccentButtonStyle}"
                            HorizontalAlignment="Center"
                            Padding="20,10" />
                </StackPanel>
            </Border>

            <!-- WebView2 -->
            <WebView2 x:Name="MarkdownWebView"
                      Visibility="Collapsed"
                      AllowDrop="True"
                      DragOver="OnDragOver"
                      Drop="OnDrop" />
        </Grid>
    </Grid>
</Window>
